CREATE TABLE IF NOT EXISTS transactionone	(
	id serial PRIMARY KEY,
	information TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS transactiontwo (
	id serial PRIMARY KEY,
	information VARCHAR (10) NOT NULL
);

